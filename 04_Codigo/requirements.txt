# TFM - Clasificacion Enfermedades Foliares Papa con ViT
# Instalar con: pip install -r requirements.txt
# PyTorch CUDA instalar por separado:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128

# Core ML
timm>=1.0.0           # PyTorch Image Models (ViT, DeiT, Swin, etc.)
scikit-learn>=1.3.0
scipy>=1.10.0         # tests estadisticos: bootstrap_ci.py, oe4_normality_check.py (Wilcoxon, Shapiro-Wilk)
numpy>=1.24.0
pandas>=2.0.0

# Computer Vision
opencv-python>=4.8.0
Pillow>=10.0.0
albumentations>=1.3.0  # Data augmentation avanzada

# XAI - Explainability
pytorch-grad-cam>=1.5.0
captum>=0.7.0          # XAI de PyTorch (alternativa)

# Hyperparameter Optimization
optuna>=3.6.0          # búsqueda HPO (optuna_runner.py). Versión usada: 4.8.0
thop>=0.1.1            # conteo de FLOPs/params (compute_model_specs.py -> model_specs.json)

# Experiment Tracking
mlflow>=2.10.0
wandb>=0.16.0          # Weights & Biases (alternativa)

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Jupyter
jupyter>=1.0.0
ipykernel>=6.25.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0
